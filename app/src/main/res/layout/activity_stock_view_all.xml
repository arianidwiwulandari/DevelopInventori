<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#B0C4DE"
    tools:context=".StockViewAllItemDescriptionActivity" >

    <TextView
        android:id="@+id/txtItemListView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/listViewStockAll"
        android:layout_alignRight="@+id/listViewStockAll"
        android:background="#FFFFFF"
        android:text="Product Description"
        android:textColor="#8B0000"
        android:textColorHint="@android:color/holo_blue_bright"
        android:textSize="20sp" />

    <ListView
        android:id="@+id/listViewStockAll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/txtItemListView"
        android:drawSelectorOnTop="true" >

    </ListView>

</RelativeLayout>